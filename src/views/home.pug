extend layouts/main
include mixin/videoBlock

block content
    .wrap.home
        each video in videos
            +videoBlock({
                _id: video._id,
                fileUrl: video.fileUrl,
                title: video.title,
                views: video.views,
                createdAt: video.createdAt,
                creator: video.creator,
            })


//- .video
//-     a(href=`${routes.videos}${routes.videoDetail(id)}`)
//-         video(src=`../${fileUrl}`)
//-         .title=title
//-         .views=`${views} views`
//-         //- -const date = video.createdAt;
//-         .createdAt=`${date.getFullYear()}년 ${date.getMonth()+1}월 ${date.getDate()}일`
//-     .creator
//-         a(href=`${routes.users}${routes.userDetail(creator)}`)
//-             i.fas.fa-user-alt
//-             span.name=` ${creatorName}`


            //- .video
            //-     a(href=`${routes.videos}${routes.videoDetail(video._id)}`)
            //-         video(src=`../${video.fileUrl}`)
            //-         .title=video.title
            //-         .views=`${video.views} views`
            //-         -const date = video.createdAt;
            //-         .createdAt=`${date.getFullYear()}년 ${date.getMonth()+1}월 ${date.getDate()}일`
            //-     .creator
            //-         a(href=`${routes.users}${routes.userDetail(video.creator)}`)
            //-             i.fas.fa-user-alt
            //-             span.name=` ${video.creatorName}`